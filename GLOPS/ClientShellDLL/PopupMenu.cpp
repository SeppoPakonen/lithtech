#include "ltbasedefs.h"
#include "ILTClient.h"

extern ILTClient* g_pLTClient;

class PopupMenu {
public:
    void AddItem();
};

void PopupMenu::AddItem() {
    // Adds a generic text item to a popup context menu
    // Extends dynamic arrays mapping item IDs to callback indices
}
